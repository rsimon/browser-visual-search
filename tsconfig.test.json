{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests run through Vitest's own transform, not tsc —
    // but we still want type-checking to cover the tests folder.
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*", "tests/**/*", "vitest.config.ts"]
}